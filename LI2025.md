# Li et al. 2025：小鼠上丘自然电影—钙响应

这个数据集包含 4 段连续自然电影和小鼠上丘浅层的双光子钙响应，适合当前项目的“同步视觉刺激 → 同步生物响应”任务。公开包无需登录，约 200 MB，许可为 CC BY 4.0。

数据包含 16 只动物、41 个成像平面和 3,414 个可靠神经元。公开元数据没有提供可靠的“成像平面到动物编号”映射，因此本项目用一个 `image_id`（一次同步成像平面）作为单体训练单位，不混合不同记录会话。

## 1. Colab 下载

```bash
%cd /content
!wget -O li_2025.zip "https://zenodo.org/records/14885567/files/li_2025.zip?download=1"
!unzip -q -o li_2025.zip -d li_2025
!find /content/li_2025 -type f | head -30
```

注意：Colab 代码单元里的 shell 命令需要以 `!` 开头；在 Colab 的 Terminal 中则不要写 `!`。

## 2. 安装项目和依赖

```bash
%cd /content/AutoMachine
!pip install -q -r requirements-li2025.txt
```

## 3. 查看可选成像平面

```bash
!python -m automachine.prepare_li2025 \
  --root /content/li_2025 \
  --inspect
```

输出会列出每个 `image_id` 的神经元数量。优先选择神经元较多的平面；不传 `--image-id` 时，处理器会自动选择最大的平面。

## 4. 处理单个成像平面

```bash
!python -m automachine.prepare_li2025 \
  --root /content/li_2025 \
  --output data/li2025/plane_one \
  --image-size 96 \
  --window 64 \
  --stride 16 \
  --max-neurons 128
```

如需指定平面，在命令中增加 `--image-id INSPECT输出的值`。默认使用灰度输入以降低显存；增加 `--rgb` 可保留彩色。

处理器会完成以下对齐：

- 响应原始顺序：猫头鹰、觅食、奔跑猫、光流；
- 视频文件顺序：猫头鹰、奔跑猫、觅食、光流；
- 将响应重排后，按每段响应的时间点从对应视频等时间采样；
- 窗口不会跨越电影边界；
- 按完整电影划分：训练为猫头鹰+奔跑猫，验证为觅食，测试为光流。

## 5. 先做小规模有效性验证

```bash
!python -m automachine.train_li2025 \
  --data data/li2025/plane_one \
  --steps 500 \
  --batch-size 2 \
  --token-dim 64 \
  --num-heads 4 \
  --eval-every 100 \
  --device cuda \
  --checkpoint checkpoints/li2025_sc_smoke.pt
```

主要观察 `val_mse < baseline`、`response_r > 0`、`delta_r > 0`，同时 `causal_max` 和 `stream_max` 应接近 0。验证有效后再启动默认 16 层、128 维 token 训练：

```bash
!python -m automachine.train_li2025 \
  --data data/li2025/plane_one \
  --steps 3000 \
  --batch-size 4 \
  --token-dim 128 \
  --num-heads 8 \
  --correlation-weight 0.25 \
  --delta-weight 0.10 \
  --device cuda \
  --checkpoint checkpoints/li2025_sc_16layer.pt
```

最优验证模型保存在指定 checkpoint；训练结束时的模型另存为 `_last.pt`，避免覆盖最优模型。

该训练默认启用256槽固定容量联想记忆。可通过
`--memory-capacity`、`--memory-value-tokens`、`--memory-top-k`、
`--memory-retrieval-threshold` 和 `--memory-write-threshold` 调整。长期槽位的在线保存方式见
[ASSOCIATIVE_MEMORY.md](ASSOCIATIVE_MEMORY.md)。

## 6. 启动可拖动时间轴的展示页

```bash
!pip install -q -r requirements-demo.txt
!python -m automachine.demo_li2025 \
  --checkpoint checkpoints/li2025_sc_16layer.pt \
  --data data/li2025/plane_one \
  --device cuda \
  --share
```

展示页直接读取连续验证/测试电影，可以拖动响应时间点，并显示当前电影帧、选定神经元曲线、全神经元热图、相关性、变化相关性、因果误差和分块流式一致性。

## 7. 保存到 Google Drive

```python
from google.colab import drive
drive.mount('/content/drive')
```

```bash
!mkdir -p /content/drive/MyDrive/AutoMachine/checkpoints
!cp /content/AutoMachine/checkpoints/li2025_sc_16layer.pt /content/drive/MyDrive/AutoMachine/checkpoints/
```

## 评估边界

验证和测试电影完全不参与训练，因此比随机窗口切分更严格。测试电影“光流”很短，测试指标方差会较大；判断训练是否有效时，应综合验证相关性、逐神经元改善比例和多次随机种子，而不是只看单次测试 MSE。
